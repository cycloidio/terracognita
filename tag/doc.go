// Package tag defines one of the ways of filtering,
// it's an abstraction of Key/Value wich each specific
// Terraform Provider implements differently, by
// just having different names (tags, labels, metadata).
package tag
